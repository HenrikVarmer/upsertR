% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upsert.R
\name{build_merge_statement}
\alias{build_merge_statement}
\title{Build a T-SQL MERGE statement}
\usage{
build_merge_statement(schema, table, keys, values, delete = FALSE)
}
\arguments{
\item{schema}{Schema that the target table resides in, e.g. \code{"dbo"}.}

\item{table}{Target table name, e.g. \code{"events"}. The staging table is
assumed to be named \verb{<table>_temp} in the same schema.}

\item{keys}{Character vector of key (matching) column names.}

\item{values}{Character vector of non-key column names to update/insert.}

\item{delete}{Logical. If \code{TRUE}, rows present in the target but absent from
the source are deleted (\verb{WHEN NOT MATCHED BY SOURCE THEN DELETE}).}
}
\value{
A single character string containing the \code{MERGE} statement.
}
\description{
Constructs the \code{MERGE} statement used to upsert the staging (\verb{*_temp}) table
into the target table. This function performs \strong{no} database I/O, which
makes it straightforward to unit test.
}
\examples{
build_merge_statement("dbo", "events", keys = "id",
                      values = c("name", "ts"), delete = TRUE)
}
