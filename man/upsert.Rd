% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upsert.R
\name{upsert}
\alias{upsert}
\title{Upsert a data frame into a SQL Server table}
\usage{
upsert(con, df, schema, table, delete = FALSE)
}
\arguments{
\item{con}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} to a SQL Server database.}

\item{df}{Data frame to upsert into the target table.}

\item{schema}{Schema that the target table resides in, e.g. \code{"dbo"}.}

\item{table}{Target table name, e.g. \code{"events"}.}

\item{delete}{Logical. If \code{TRUE}, rows present in the target but absent from
\code{df} are deleted. Defaults to \code{FALSE}.}
}
\value{
Invisibly, the \code{MERGE} statement that was executed.
}
\description{
Writes \code{df} to a staging table (\verb{<table>_temp}), introspects the target
table's key and value columns from \code{INFORMATION_SCHEMA}, and executes a
T-SQL \code{MERGE} to upsert the staged rows into the target table. The write,
merge and truncate are wrapped in a single transaction so that a failure
does not leave a half-populated staging table behind.
}
\examples{
\dontrun{
con <- DBI::dbConnect(odbc::odbc(), "my-dsn")
upsert(con, df = events, schema = "dbo", table = "events", delete = TRUE)
}
}
